#include "holberton.h"
/**
 *int _islower - funtion that checks for lowercase
 *c - print functions for alphabet
 *Return: always 0 (success)
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
	{
return (1);
	}
else
return (0);
}
